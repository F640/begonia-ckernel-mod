diff --git a/kernel/kernel_umount.c b/kernel/kernel_umount.c
index d4cbdd59..e3543982 100644
--- a/kernel/kernel_umount.c
+++ b/kernel/kernel_umount.c
@@ -417,7 +417,7 @@ do_umount:
     susfs_reorder_mnt_id();
 #endif // #ifdef CONFIG_KSU_SUSFS_SUS_MOUNT
 
-    susfs_set_current_proc_umounted();
+    susfs_set_current_non_root_user_app_proc();
 
     put_task_struct(current);
 
diff --git a/kernel/setuid_hook.c b/kernel/setuid_hook.c
index 7fb1b69b..fafe8b15 100644
--- a/kernel/setuid_hook.c
+++ b/kernel/setuid_hook.c
@@ -179,7 +179,7 @@ int ksu_handle_prctl(int option, unsigned long arg2, unsigned long arg3,
 #ifdef CONFIG_KSU_SUSFS
     // - We straight up check if process is supposed to be umounted, return 0 if so
     // - This is to prevent side channel attack as much as possible
-    if (likely(susfs_is_current_proc_umounted()))
+    if (likely(susfs_is_current_non_root_user_app_proc()))
         return 0;
 #endif
 
